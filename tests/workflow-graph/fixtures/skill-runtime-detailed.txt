(1) 接收 skill 运行请求 :: 解析用户目标与约束
    [1.1] 目标类型
          - workflow
          - 机制
    [1.2] 输出偏好
          - 简版
          - 详细版
└─ (2) 模式是否已声明? :: 决定渲染策略
   [2.1] 用户显式指定
         - 使用指定模式
   [2.2] 用户未指定
         - 按复杂度自动判断
   ├─ Yes -> (3) 渲染图本体 :: 只输出图结构
   │          [3.1] 节点规范
   │                - `(n) 标题 :: 一行解释`
   │          [3.2] 循环规范
   │                - `[LOOP Lx -> (n)]`
   └─ No  -> (4) 自动判定复杂度 :: 选择简版或详细版 -> [LOOP L2 -> (2)]
